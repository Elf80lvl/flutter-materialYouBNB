import 'package:flutter/material.dart';

class MoreProfileTab extends StatefulWidget {
  const MoreProfileTab({Key? key}) : super(key: key);

  @override
  _MoreProfileTabState createState() => _MoreProfileTabState();
}

class _MoreProfileTabState extends State<MoreProfileTab> {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Profile Tab'),
    );
  }
}
